name: Main branch workflow

on:
   push:
       branches:
           - main


jobs:  
  buils-main:
   runs-on: ubuntu-latest

   steps:
     - name: checkout code
       uses: actions/checkout@v3

     - name: run a script
       run: echo "this run only on the mian"

      
      
