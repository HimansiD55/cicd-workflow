name: Print Message on Push to newb

on:
  push:
    branches:
      -  "newb/*"

jobs:
  new-branch:
    runs-on: ubuntu-latest

    steps: 
      - name: checkout code
        uses: action/checkout@v3

      -  name: run tests
         run: echo "running test from newb brancceeeeeeeh "
         

