name: Print Message on Push to Main

on:
  push:
    branches:
      - main

jobs:
  print-message-job:
    runs-on: ubuntu-latest

    steps:
      - name: Print a message to the console
        run: echo "Hello GitHub Actions"

